{
  "name": "ballpov",
  "version": "0.1.0",
  "type": "module",
  "description": "A 3D browser game where you are the ball.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf assets && cp dist/index.dev.html index.html && cp -r dist/assets ./assets"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.3.0"
  }
}
